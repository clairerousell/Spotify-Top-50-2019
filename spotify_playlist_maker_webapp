## this is way harder than anticipated... idk if I can do it rip

from flask import Flask, render_template, request
import spotify_info
app = Flask(__name__)

@app.route('/')
@app.route('/main')
def main():)
def playlist_maker():

    return




if __name__ == '__main__':
    app.run('0.0.0.0',port=3000)
